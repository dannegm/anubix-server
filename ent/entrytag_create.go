// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/dannegm/anubix-server/ent/entry"
	"github.com/dannegm/anubix-server/ent/entrytag"
	"github.com/dannegm/anubix-server/ent/tag"
)

// EntryTagCreate is the builder for creating a EntryTag entity.
type EntryTagCreate struct {
	config
	mutation *EntryTagMutation
	hooks    []Hook
}

// SetEntryID sets the "entry" edge to the Entry entity by ID.
func (_c *EntryTagCreate) SetEntryID(id string) *EntryTagCreate {
	_c.mutation.SetEntryID(id)
	return _c
}

// SetEntry sets the "entry" edge to the Entry entity.
func (_c *EntryTagCreate) SetEntry(v *Entry) *EntryTagCreate {
	return _c.SetEntryID(v.ID)
}

// SetTagID sets the "tag" edge to the Tag entity by ID.
func (_c *EntryTagCreate) SetTagID(id string) *EntryTagCreate {
	_c.mutation.SetTagID(id)
	return _c
}

// SetTag sets the "tag" edge to the Tag entity.
func (_c *EntryTagCreate) SetTag(v *Tag) *EntryTagCreate {
	return _c.SetTagID(v.ID)
}

// Mutation returns the EntryTagMutation object of the builder.
func (_c *EntryTagCreate) Mutation() *EntryTagMutation {
	return _c.mutation
}

// Save creates the EntryTag in the database.
func (_c *EntryTagCreate) Save(ctx context.Context) (*EntryTag, error) {
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *EntryTagCreate) SaveX(ctx context.Context) *EntryTag {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *EntryTagCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *EntryTagCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *EntryTagCreate) check() error {
	if len(_c.mutation.EntryIDs()) == 0 {
		return &ValidationError{Name: "entry", err: errors.New(`ent: missing required edge "EntryTag.entry"`)}
	}
	if len(_c.mutation.TagIDs()) == 0 {
		return &ValidationError{Name: "tag", err: errors.New(`ent: missing required edge "EntryTag.tag"`)}
	}
	return nil
}

func (_c *EntryTagCreate) sqlSave(ctx context.Context) (*EntryTag, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *EntryTagCreate) createSpec() (*EntryTag, *sqlgraph.CreateSpec) {
	var (
		_node = &EntryTag{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(entrytag.Table, sqlgraph.NewFieldSpec(entrytag.FieldID, field.TypeInt))
	)
	if nodes := _c.mutation.EntryIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   entrytag.EntryTable,
			Columns: []string{entrytag.EntryColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(entry.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.entry_entry_tags = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := _c.mutation.TagIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   entrytag.TagTable,
			Columns: []string{entrytag.TagColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(tag.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.tag_entry_tags = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// EntryTagCreateBulk is the builder for creating many EntryTag entities in bulk.
type EntryTagCreateBulk struct {
	config
	err      error
	builders []*EntryTagCreate
}

// Save creates the EntryTag entities in the database.
func (_c *EntryTagCreateBulk) Save(ctx context.Context) ([]*EntryTag, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*EntryTag, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*EntryTagMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *EntryTagCreateBulk) SaveX(ctx context.Context) []*EntryTag {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *EntryTagCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *EntryTagCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
